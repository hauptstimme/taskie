%h2 Tasks

- if @tasks.any?
  %table.table
    %thead
      %tr
        %th Status
        %th Name
        %th Assigned to
        %th Created
    %tbody
      - @tasks.each do |task|
        %tr
          %td= status_label_for task
          %td= link_to task.name||"—", task
          %td
            - if task.assignee.present?
              = gravatar_for(task.assignee) + task.assignee.email
            - else
              nobody
          %td= task.created_at.to_s(:long)

%hr

= link_to "New task", new_task_path, class: "btn btn-primary"
